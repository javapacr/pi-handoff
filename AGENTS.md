# pi-handoff

Pi extension — session handoff: `/handoff` (detached doc generation), `/continue` (new session from a doc path or bare text), and the `continue` tool. Installed via git in both profiles.

- **Pending work**: [docs/backlog.md](docs/backlog.md) — index to per-item docs in `docs/backlog/`. No backlog content lives anywhere else.
- README.md carries usage, config, and architecture detail. `skills/pi-handoff/SKILL.md` is the shipped skill.
